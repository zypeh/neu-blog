{
  "name": "neu-blog",
  "version": "0.1.0",
  "description": "blog",
  "main": "index.js",
  "repository": "https://github.com/zypeh/zypeh.github.io",
  "author": "zypeh <zypeh.geek@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "dev": "node server.js",
    "build": "next build",
    "start": "NODE_ENV=production node server.js",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ && git commit -m \"Deploy to branch gh-pages\" && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "@mdx-js/mdx": "^0.16.0",
    "@zeit/next-mdx": "^1.2.0",
    "next": "latest",
    "preact": "^8.3.1",
    "preact-compat": "^3.18.4",
    "remark-emoji": "^2.0.2",
    "remark-images": "^0.16.0"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.0",
    "react": "^16.6.3",
    "react-dom": "^16.6.3"
  }
}
